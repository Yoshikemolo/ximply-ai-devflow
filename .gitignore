# Operating system and editor artifacts
Thumbs.db
Desktop.ini
.DS_Store
*~

# Microsoft Office lock and temporary files
~$*
*.tmp

# Private AI configuration and local agent state.
# See the framework section "Private AI Configuration": these locations may
# accumulate credentials, internal URLs, local paths and private prompts.
# Reviewed, reusable engineering instructions belong under /docs/ai instead.
.claude/*
!.claude/skills/
!.claude/settings.json
.cursor/
.settings/
.local-ai/
.ai-private/

# Python bytecode and caches. Generated by the corpus tooling; never source.
__pycache__/
*.py[cod]

# Secrets and local environment
.env
.env.*
!.env.example
*.local.json
